# คำแนะนำสำหรับ AI agent ที่แก้โค้ดในโปรเจคนี้

โปรเจคแปลภาษามือไทยเป็นข้อความ มี **สองฝั่งที่ต้องสอดคล้องกันตลอดเวลา**

| ฝั่ง | โฟลเดอร์ | ภาษา |
|------|----------|------|
| คอมพิวเตอร์ | `src/` | Python (OpenCV + MediaPipe + scikit-learn) |
| มือถือ (PWA) | `app/` | JavaScript ล้วน ไม่มี build step ไม่มี npm |

---

## กฎเหล็ก — ผิดข้อไหนถือว่างานยังไม่เสร็จ

### 1. `src/features.py` กับ `app/js/features.js` ต้องแก้คู่กันเสมอ

สองไฟล์นี้เป็นโค้ดคนละภาษาที่ต้องให้ผลลัพธ์ตรงกันทุกหลัก (ต่างกันได้ไม่เกิน `1e-6`)
เพราะชุดข้อมูลถูกแชร์ระหว่างสองฝั่ง **แก้ไฟล์เดียวโดยไม่แก้อีกไฟล์ = ข้อมูลใช้ร่วมกันไม่ได้
โดยไม่มี error ฟ้องเลย** โมเดลจะแค่แม่นยำต่ำลงอย่างอธิบายไม่ถูก ซึ่งหาสาเหตุยากมาก

แก้แล้วต้องรัน และต้องผ่านทุกกรณี:

```bash
run parity
```

### 2. เปลี่ยน `FEATURE_DIMS` = เปลี่ยนรูปแบบข้อมูล ต้องทำให้ครบชุด

ถ้าเพิ่ม/ลดจำนวนมิติของ feature vector ต้องแก้ทั้งหมดนี้พร้อมกัน:

- `src/features.py` และ `app/js/features.js` (ทั้งการสร้าง การกลับด้าน และชื่อคอลัมน์)
- เพิ่มเลข `DATASET_VERSION` ทั้ง `src/transfer.py` และ `app/js/transfer.js`
- เขียนทางย้ายข้อมูลรุ่นเก่าทั้งสองฝั่ง (ดูตัวอย่าง `upgrade_legacy_vector`)
- เพิ่ม `DB_VERSION` ใน `app/js/storage.js` พร้อมย้ายข้อมูลใน `onupgradeneeded`
- ทิ้งโมเดลเก่าที่ `inputDim` ไม่ตรง ไม่งั้นมันจะทายต่อได้เงียบ ๆ ด้วยข้อมูลผิดขนาด
- อัปเดตเลขมิติในเอกสาร: `README.md`, `app/README.md`, `docs/คู่มือทำความเข้าใจ.md`

### 3. คอนเวนชันกลางของเวกเตอร์คือ "ภาพดิบจากกล้อง ยังไม่พลิกกระจก"

ฝั่งคอมพิวเตอร์พลิกเฟรมด้วย `cv2.flip` ก่อนส่งเข้า MediaPipe จึงต้องแปลงเวกเตอร์กลับด้วย
`build_canonical_feature_vector` เสมอ **ห้ามเรียก `build_feature_vector` ตรง ๆ ใน
`collect.py` หรือ `translate.py`** ฝั่งมือถือส่งภาพดิบเข้าไปอยู่แล้ว (พลิกกระจกด้วย CSS
ตอนแสดงผลเท่านั้น) จึงไม่ต้องแปลง

### 4. แบ่งชุดข้อมูลก่อน แล้วค่อยเพิ่มข้อมูลกลับด้าน

การเพิ่มข้อมูลแบบกลับด้านซ้าย-ขวา (`mirror`) ต้องทำ **หลัง** แบ่งชุดตรวจสอบออกไปแล้ว
และตัวปรับสเกลต้อง fit จากชุดเทรนเท่านั้น ไม่งั้นความแม่นยำที่รายงานจะสูงเกินจริง
ดู `src/train.py` และ `app/js/mlp.js`

### 5. ห้ามแตะ `app/vendor/` และ `app/models/`

เป็นไฟล์ MediaPipe ที่ถูก vendor ไว้ (รวมกัน 19 MB) ตั้งใจ commit ขึ้น git เพื่อให้
GitHub Pages เสิร์ฟได้และแอปทำงานออฟไลน์ได้ ถ้าเปลี่ยนไฟล์กลุ่มนี้ต้องเพิ่มเลข
`CACHE_VERSION` ใน `app/sw.js` ด้วย (ไฟล์กลุ่มนี้แคชแบบ cache-first ไม่ตรวจซ้ำ)

### 6. ข้อมูลผู้ใช้อยู่ในเครื่องเท่านั้น

ห้ามเพิ่มการส่งภาพ ข้อมูล landmark หรือ telemetry ออกนอกเครื่องไม่ว่ากรณีใด
แอปต้องทำงานได้เต็มรูปแบบขณะออฟไลน์ ห้ามเพิ่ม CDN หรือ dependency ที่โหลดตอนรัน

---

## สไตล์โค้ด

- **คอมเมนต์เป็นภาษาไทย อธิบาย "ทำไม" ไม่ใช่ "ทำอะไร"** โปรเจคนี้เป็นสื่อการเรียนรู้
  คอมเมนต์ที่ดีคือคอมเมนต์ที่บอกเหตุผลของการตัดสินใจและกับดักที่เคยเจอ
- ชื่อตัวแปรและฟังก์ชันเป็นภาษาอังกฤษ ข้อความที่ผู้ใช้เห็นเป็นภาษาไทย
- `run.bat` ต้องเป็น ASCII ล้วน (cmd.exe อ่านไฟล์ .bat ด้วย console code page ภาษาไทยจะเพี้ยน)
- ฝั่งมือถือเป็น JavaScript ล้วน ไม่มี bundler **ห้ามเพิ่ม npm dependency หรือ build step**
- ข้อความภาษาไทยบนภาพต้องวาดด้วย `src/thai_text.py` (Pillow) เพราะ `cv2.putText` รองรับแค่ ASCII

---

## ตรวจงานก่อนบอกว่าเสร็จ

```bash
run selftest    # 13 ข้อ ฝั่ง Python ไม่ต้องใช้กล้อง
run parity      # Python กับ JavaScript ให้เวกเตอร์ตรงกัน (ต้องมี Node.js)
run jstest      # โมเดลฝั่ง JavaScript
```

ถ้าแก้อะไรที่กระทบ feature vector หรือรูปแบบข้อมูล **ต้องรันทั้งสามคำสั่ง**

หมายเหตุ: `run.bat` ต้องการ `.venv` ในรากโปรเจค ถ้าไม่มีให้บอกผู้ใช้ อย่าสร้างเอง
และอย่ารายงานว่าเทสต์ผ่านถ้ายังไม่ได้รันจริง
